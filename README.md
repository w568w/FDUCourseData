# FDUCourseData

Fudan University course data crawler.

## Usage

```bash
uv run python crawler.py -s 504 -o courses.json
```

- `-s, --semester-id`: Semester ID (required)
- `-o, --output`: Output file path (default: courses.json)
